import 'package:flutter/material.dart';
import 'package:sign_in/sign_in.dart';

class ModularTodo extends StatelessWidget {
  const ModularTodo({super.key});

  @override
  Widget build(BuildContext context) {
    return const SignInScreen();
  }
}
